# a line-by-line list of directories/files to exclude.
{% if (resticbk_exclude_list is defined) and resticbk_exclude_list is iterable and resticbk_exclude_list|length > 0 %}
{% for item in resticbk_exclude_list %}
{{ item }}
{% endfor %}
{% endif %}

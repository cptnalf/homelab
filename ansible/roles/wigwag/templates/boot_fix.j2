#!/bin/sh

version="${1}"
# version is required.
[ -z "${version}" ] && exit 0

cd /sdcard_boot

# backup current booting kernel.
cp vmlinuz-sunxi /boot/vmlinuz.old
cp initrd.img-sunxi /boot/initrd.img.old

cp /boot/vmlinuz-${version} vmlinuz-sunxi
cp /boot/initrd.img-${version} initrd.img-sunxi
